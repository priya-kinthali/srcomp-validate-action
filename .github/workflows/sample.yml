name: main

on:
  push:
    branches:
      - test-action-361
  pull_request:

jobs:
  self-test-happy-path:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      # uses: actions/setup-python@v5
      # uses: priya-kinthali/setup-python@sample-361-multi
      # uses: actions/setup-python@main
      uses: gowridurgad/setup-python@pip-install
      with:
        python-version: '3.10'
        cache: pip
        # cache-dependency-path: |
        #   demo/subdemo/setup.cfg
        #   subdirectory/requirements-dev.txt
        #   **/requirements.txt  
        pip-install: -r  demo/subdemo/requirements-sample.txt

    # - name: Install Dependencies
    #   shell: bash
    #   run: pip install -r requirements-sample.txt

    - name: Python Version
      shell: bash
      run: python --version
          # requirements-dev.txt
          # requirements-test.txt
          #          requirements.txt
          #           **/setup.cfg
          # **/requirements.txt
          # **/requirements-dev.txt